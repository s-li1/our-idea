export default {
    // User table in firebase
    USERS: "users",
    // Projects table in firebase
    PROJECTS: "projects",
    // Matches table (mapping users to project ids)
    MATCHES: "matches"
}