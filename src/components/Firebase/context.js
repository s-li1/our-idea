import React from 'react';

/** @type {React.Context<import('../../clients/ChatAppClient/ChatAppClient').default>} */
const FirebaseContext = React.createContext(null);

export default FirebaseContext;